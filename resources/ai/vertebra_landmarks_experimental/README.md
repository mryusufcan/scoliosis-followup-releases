# V2 Landmark ONNX Candidate (Quarantine Only)

This package is a technically converted ONNX candidate for 17 decoder rows and 68 vertebra-corner landmarks. It is **not active**, **not clinically validated**, and **not accepted for expert-review POC**.

The acceptance report deliberately fails because patient-level validation metrics and declared weights/dataset rights are absent. Do not copy this folder into the application's active model location.
